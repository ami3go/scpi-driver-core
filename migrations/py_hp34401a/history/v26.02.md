# History — v26.02

## Added

- Unified `Connect DMM` keyword with explicit AUTO/VISA/SERIAL selection.
- Unified `Disconnect DMM` keyword.
- Runtime `Get Driver Metadata` and `Get Driver Capabilities` keywords.
- Non-Robot Python compatibility aliases for existing multi-driver orchestration libraries.
- Canonical RFDS-017 v3.0 `ai/ai_contract.yaml` describing all 69 Robot keywords.
- SHA-256 `ai/ai_contract.lock` for public-interface staleness detection.
- Contract validator and tests.
- RFDS-018 bench-contract template and setup guide.

## Changed

- Package version advanced from 26.1.0 to 26.2.0.
- Strict stable-resistance keyword now has an explicit signature suitable for deterministic test generation.
- README, GitHub Pages, keyword reference, architecture, release notes, CI, package manifests, and release tooling updated.

## Compatibility

- No v26.01 Robot keyword was removed or renamed.
- Existing VISA, serial, and simulator workflows remain valid.
- Legacy `MatrixVerificationLibrary.py` code can call `connect_to_dmm()` without a separate monkey-patch file.

## Qualification

- Software, simulation, contract, documentation, and packaging validation are required for release.
- Physical HIL, long-duration behavior, and site-specific bench approval remain separate qualification gates.
