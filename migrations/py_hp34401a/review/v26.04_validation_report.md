# v26.04 validation report

## Candidate

- Driver: `rf_hp34401a`
- Release: `26.04`
- Distribution: `26.4.0`
- Release class: **D0 — development**
- Intended archive: `rf_hp34401a_v26.04.zip`
- Stable internal root: `rf_hp34401a/`

## Executed validation

| Validation | Result | Evidence |
|---|---|---|
| Python core and adapter regression | PASS — 112 tests | local pytest execution |
| Guarded physical Python tests | SKIP — 2, prerequisites absent | explicit HIL guards |
| Adapter package line coverage | PASS — 95% | coverage report for `rf_hp34401a/*` |
| Python compilation | PASS | `compileall` / `py_compile` |
| Canonical simulation smoke | PASS — 12.0 V deterministic reading | direct Python call through public library |
| RFDS-002/RFDS-017 synchronization | PASS — 108 keywords | `scripts/validate_ai_contract.py` |
| RFDS-019 static inventory/vector coverage | PASS — 108/108 | `scripts/validate_call_protocol_conformance.py` |
| Real-hardware all-API source inventory | PASS — 108/108 names | `scripts/validate_real_hardware_api_suite.py` |
| RFDS-014 schema/default mirrors and schema lock | PASS | byte comparison and SHA-256 verification |
| JSON and YAML parse validation | PASS | all maintained machine-readable files |
| Plugin metadata and unconnected factory smoke | PASS | direct provider test |
| Wheel and source distribution build | PASS | `dist/` build during release assembly |
| Stable ZIP root and prohibited-content validation | PASS after final assembly | `scripts/build_release.py` |

## Not executed in this build environment

The build environment does not provide the official Robot Framework, Ruff, Mypy, or MkDocs runtimes. Therefore the following are **not claimed as passed**:

- official Robot Framework software suites and generated `output.xml`, `log.html`, and `report.html`;
- official RFDS-019 runtime conformance execution;
- official Libdoc regeneration;
- MkDocs strict build;
- Ruff and Mypy gates;
- physical VISA/GPIB or RS-232 HIL;
- zero-exclusion all-public-API hardware qualification.

## Hardware suite

The package contains `tests/hil/verify_all_public_api_real_hardware.robot`. It:

- requires `HIL_ENABLED:true`;
- requires an explicit real VISA resource;
- never falls back to simulation after a real connection failure;
- accounts for all 108 exported keywords;
- records PASS, FAIL, EXCLUDED, or NOT RUN for each public keyword;
- emits device identity, environment, coverage, and Robot evidence;
- requires separate authorization for current, resistance, diode, reset, self-test, raw-I/O, trigger, and serial profiles;
- can fail on every exclusion for a full claimed-scope qualification.

## Verdict

**D0 development candidate suitable for release-site Robot validation and controlled real-hardware qualification.** It is not a D2 hardware-validated or P1 production-approved release.
