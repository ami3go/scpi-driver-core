# Linux VISA/GPIB setup

Install a supported VISA backend and the driver required by the selected GPIB adapter. Install the project VISA extra, verify resource enumeration, and run the VISA example with an explicit resource. Backend capabilities vary; validate timeout, clear, and interface-clear behavior before production use.
