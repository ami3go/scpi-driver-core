# Configuration profiles

Use `Validate Driver Configuration` before applying a profile. Use `Import Driver Configuration` with `validate_only=true` to guarantee no runtime change. Profiles are persisted only by `Save Driver Configuration`; package files remain read-only.
