# History — v26.04

Date: 2026-07-30  
Release class: D0 — development candidate

## Public API

- Added the RFDS-002 v1.1 canonical lifecycle keywords: `Connect`, `Disconnect`, `Is Connected`, `Get Connection State`, `Check Communication`, `Get Identity`, `Get Driver Information`, `Get Driver Capabilities`, `Set Communication Timeout`, and `Get Communication Timeout`.
- Added canonical multi-connection and device-error keyword groups.
- Added RFDS-013 capability discovery keywords and retained a separate detailed capability-model call to resolve the RFDS-002/RFDS-013 return-schema conflict.
- Added RFDS-014 configuration validation, import/export, profile, reset, and schema keywords.
- Added explicit raw-I/O authorization and guarded raw write/query/read calls.
- Preserved all v26.03 DMM-specific Robot keywords and Python matrix-orchestration compatibility calls.

## Architecture and safety

- Added structured RFDS-007 exception codes, diagnostics, causal chaining, retryability, recovery guidance, and post-failure state.
- Improved case-insensitive alias normalization, deterministic active-session behavior, idempotent canonical disconnect, and partial-close error collection.
- Fixed canonical simulation reconnect idempotency by preserving the caller-selected simulation resource.
- Fixed JSON-string configuration import so long JSON documents are not interpreted as filesystem paths.
- Guarded legacy raw SCPI methods with the same explicit opt-in policy as canonical raw I/O.
- Added Robot-time-string conversion fallback independent of Robot internal utility imports.

## Machine-readable integration

- Expanded RFDS-002 `public_api.yaml`, RFDS-017 AI contract, interface lock, and RFDS-019 inventory/vectors to 108 exported keywords.
- Added RFDS-013 capability model, schema, snapshot, query example, validation tests, and documentation.
- Added RFDS-014 configuration schema/lock/default/example/simulator profile, runtime resources, tests, and guides.
- Added RFDS-015 plugin provider, manifest, Python entry point, environment validation, tests, and documentation.
- Added release-class and controlled-deviation records for pending `rfds-core` and RFDS-004 v2 transport migration.

## Real hardware verification

- Added `tests/hil/verify_all_public_api_real_hardware.robot`.
- Added all-public-API coverage listener and JSON/CSV/Markdown/environment/device-identity evidence.
- Added fail-closed HIL profile and hardware-resource inventory templates with explicit `UNKNOWN` fields for bench-owned facts.
- Added timestamped BAT, PowerShell, and shell runners.
- Added separate opt-in profiles for DC voltage, AC voltage, current, resistance/stability, frequency/period, continuity/diode, trigger, self-test, reset, raw I/O, and RS-232.
- Every exported keyword is represented in the HIL source and receives PASS, FAIL, EXCLUDED, or NOT RUN status.
- No HIL profile silently falls back to simulation.

## Documentation and examples

- Updated README and GitHub Pages sources to v26.04.
- Added all-public-API hardware verification documentation and guide.
- Added numbered example 13 for canonical lifecycle, capability discovery, configuration, and simulation.
- Added `examples/index.yaml`.

## Validation performed in the build environment

- 112 Python tests passed; 2 guarded physical tests skipped.
- Combined `rf_hp34401a` coverage: 95%.
- RFDS-002/RFDS-017 synchronization: PASS, 108 keywords.
- RFDS-019 static inventory/vector validation: PASS, 108 keywords.
- Python compilation and configuration artifact consistency: PASS.
- Corrected and verified the RFDS-014 schema lock against the canonical schema SHA-256.

## Pending external evidence

- Official Robot Framework execution of the updated Robot suites.
- Official Libdoc regeneration and strict MkDocs execution in the release-site uv environment.
- Physical VISA/GPIB and RS-232 all-API verification.
- Adoption of the shared RFDS v2 base/transport packages when available.
