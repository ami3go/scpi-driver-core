# History — v26.05

## Fixed

- Corrected the all-public-API real-hardware Robot suite failure `Evaluating expression 'true' failed`.
- Added `Normalize Boolean Value` in the HIL coverage helper.
- Normalized `HIL_ENABLED`, `FAIL_ON_EXCLUSIONS`, and all eleven `RUN_*_PROFILE` variables during suite setup.
- Accepted case-insensitive Boolean strings and rejected unknown values before hardware access.
- Moved coverage start after mandatory HIL/resource preflight.
- Added `COVERAGE_STARTED` guarding so setup failures do not cause a misleading teardown inventory failure.
- Updated HIL runner scripts to pass `True`/`False` instead of lowercase strings.

## Compatibility

- No public Robot keyword was added, removed, renamed, or reordered.
- RFDS-002, RFDS-017, and RFDS-019 remain synchronized at 108 public keywords.
- Existing hardware profile variables and command lines remain compatible; lowercase Boolean values are now handled correctly.

## Validation

- Python compilation passed.
- Boolean normalization tests passed for supported true and false spellings.
- RFDS-002/RFDS-017 validation passed for 108 keywords.
- RFDS-019 static inventory/vector validation passed for 108 keywords.
- Real-hardware suite static inventory validation passed for 108 keywords.
- Official Robot execution and physical HP34401A verification remain release-site actions.
