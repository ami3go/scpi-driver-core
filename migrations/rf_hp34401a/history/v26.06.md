# History — v26.06

## Fixed

- Corrected the all-public-API real-hardware coverage state loss observed after successful VISA tests.
- Added `RealHardwareApiListener.py`, registered explicitly by the official HIL launchers.
- Added atomic file-backed result state shared by the listener, exclusion helper, and finalizer.
- Registered every disabled profile exclusion during suite setup.
- Replaced disabled-profile `SKIP` termination with `Pass Execution` after explicit EXCLUDED records are created.
- Made finalization return and log a complete summary before a separate acceptance assertion can fail teardown.

## Compatibility

- No public driver keyword was added, removed, renamed, or reordered.
- The RFDS-002/RFDS-017/RFDS-019 inventory remains 108 public keywords.
- Existing VISA, serial, simulation, configuration, capability, and measurement behavior is unchanged.
