# v26.03 Gate 1 — Architecture and skeleton review

## Decision

Use `hp34401a_dmm.FakeTransport` as the approved unattended protocol observation point. It is the same line-oriented SCPI boundary used by the real VISA and serial transports and therefore validates serialized commands and raw responses rather than merely mocking adapter method calls.

## Delivered

- complete RFDS-019 folder skeleton;
- deterministic keyword-inventory and vector schemas;
- response-schema location;
- risk and device-facing classification;
- real-device exclusion records;
- runner and evidence-layout design.

## Review result

PASS. No Critical or Major architecture findings remain. Real-device traces are intentionally outside the unattended profile and remain a HIL prerequisite.
