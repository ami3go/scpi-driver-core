# rf_hp34401a v26.06 validation report

## Trigger

A physical HP34401A run on v26.05 passed VISA discovery, connection, identity, health/error/recovery, simulator-isolation, and cleanup tests, but teardown reported executed and excluded APIs as `NOT RUN`.

## Root cause

The HIL evidence helper relied on an embedded listener and in-memory result state. That state was not a reliable observation path for nested public driver keywords in the user's Robot runtime. Profile exclusions were also registered inside tests immediately before test-level `SKIP`, making the final evidence dependent on runtime scope behavior.

## Corrective validation

- RFDS-002/RFDS-017 synchronized API validation: PASS, 108 keywords.
- RFDS-019 inventory/vector static validation: PASS, 108/108.
- Default HIL profile static accounting: PASS, every keyword directly invoked or registered as EXCLUDED.
- File-backed state regression: PASS, 108/108 records persisted.
- Listener precedence regression: PASS, an executed PASS overrides a prior EXCLUDED record.
- Core Python tests: 67 PASS, 2 physical tests SKIP through explicit environment guards.
- Python compilation: PASS.
- Wheel and source distribution build: PASS.
- Final ZIP root and prohibited-content validation: PASS.

## Pending

Robot Framework is unavailable in the package-build container. The corrected v26.06 HIL launcher must be rerun in the user's uv environment. Fixture-dependent profiles remain pending safe bench enablement.

## Verdict

Approved as a backward-compatible D0 hotfix. No public API or SCPI behavior changed.
