# Hardware sweep allowlist for py_hp34401a.
#
# Generated by generate_allowlist.py, which proposes only methods the
# source proves send nothing but queries. Regenerating overwrites the
# proposal but never the signature block, so review the diff.
#
# The sweep refuses to run while signed_off_by is empty. Signing means:
# you have read the read_only list, you know what is connected to this
# instrument, and you accept these calls being made to it.

driver = "py_hp34401a"

# Proposed by static analysis: public, no arguments, sends only queries,
# and not name-matched as dangerous. 5 methods.
read_only = [
    "fetch",  # FETCh?
    "identify",  # *IDN?
    "query_terminal",  # ROUTe:TERMinals?
    "read_error",  # SYSTem:ERRor?
    "read_query",  # READ?
]

# Not called by the sweep. Each either writes to the instrument, sends
# nothing this script can see, or is name-matched as dangerous. Promote
# one into read_only only deliberately, and only with a reason recorded
# here. 42 methods.
needs_review = [
    # "apply_runtime_policy",  # dangerous name
    # "assert_no_error",  # no visible command
    # "clear",  # dangerous name
    # "clear_status",  # writes
    # "close",  # writes
    # "configure_continuity",  # dangerous name
    # "configure_diode",  # dangerous name
    # "connect",  # dangerous name
    # "drain_error_queue",  # no visible command
    # "emit_event",  # no visible command
    # "export_diagnostic_bundle",  # no visible command
    # "finalize",  # no visible command
    # "heartbeat",  # no visible command
    # "identity_cached",  # no visible command
    # "initiate",  # writes
    # "is_connected",  # dangerous name
    # "is_no_error",  # no visible command
    # "is_open",  # no visible command
    # "lock",  # dangerous name
    # "log_protocol",  # no visible command
    # "measure_2wire_resistance",  # no visible command
    # "measure_4wire_resistance",  # no visible command
    # "measure_ac_voltage",  # no visible command
    # "measure_dc_current",  # no visible command
    # "measure_dc_voltage",  # no visible command
    # "name",  # no visible command
    # "open",  # no visible command
    # "pass_fail",  # no visible command
    # "read_once",  # writes
    # "read_once_bus",  # writes
    # "read_raw",  # no visible command
    # "read_raw_response",  # no visible command
    # "record_device_identity",  # no visible command
    # "record_error",  # no visible command
    # "recover",  # dangerous name
    # "reset",  # writes
    # "self_test",  # dangerous name
    # "state",  # no visible command
    # "transport_name",  # no visible command
    # "transport_type",  # no visible command
    # "trigger_bus",  # writes
    # "unit",  # no visible command
]

[sign_off]
signed_off_by = ""      # your name; the sweep will not run without it
signed_off_date = ""    # ISO date
instrument_serial = ""  # which physical unit this was signed for
allow_writes = false    # leave false; see README before changing
