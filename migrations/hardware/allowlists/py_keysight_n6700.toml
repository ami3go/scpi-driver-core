# Hardware sweep allowlist for py_keysight_n6700.
#
# Generated by generate_allowlist.py, which proposes only methods the
# source proves send nothing but queries. Regenerating overwrites the
# proposal but never the signature block, so review the diff.
#
# The sweep refuses to run while signed_off_by is empty. Signing means:
# you have read the read_only list, you know what is connected to this
# instrument, and you accept these calls being made to it.

driver = "py_keysight_n6700"

# Proposed by static analysis: public, no arguments, sends only queries,
# and not name-matched as dangerous. 20 methods.
read_only = [
    "channel_count",  # SYST:CHAN:COUN?
    "fetch_current",  # FETC:CURR? {}
    "fetch_voltage",  # FETC:VOLT? {}
    "get_current_limit",  # CURR? {}
    "get_current_range",  # CURR:RANG? {}
    "get_error",  # SYST:ERR?
    "get_input",  # SIM:LOAD:INP? {}
    "get_ocp",  # CURR:PROT:STAT? {}
    "get_operation_status",  # STAT:OPER:COND? {}
    "get_ovp",  # VOLT:PROT? {}
    "get_questionable_status",  # STAT:QUES:COND? {}
    "get_smu_mode",  # FUNC:MODE? {}
    "get_voltage_range",  # VOLT:RANG? {}
    "get_voltage_setpoint",  # VOLT? {}
    "idn",  # *IDN?
    "measure_current",  # MEAS:CURR? {}
    "measure_voltage",  # MEAS:VOLT? {}
    "operation_complete",  # *OPC?
    "standard_event_status",  # *ESR?
    "status_byte",  # *STB?
]

# Not called by the sweep. Each either writes to the instrument, sends
# nothing this script can see, or is name-matched as dangerous. Promote
# one into read_only only deliberately, and only with a reason recorded
# here. 46 methods.
needs_review = [
    # "channels",  # no visible command
    # "check_errors",  # no visible command
    # "clear",  # dangerous name
    # "clear_protection",  # writes
    # "clear_status",  # writes
    # "client",  # no visible command
    # "close",  # dangerous name
    # "configure_load_cc",  # dangerous name
    # "configure_smu_current_priority",  # dangerous name
    # "configure_smu_voltage_priority",  # dangerous name
    # "connect_ethernet",  # dangerous name
    # "connect_simulated",  # dangerous name
    # "connect_usb",  # dangerous name
    # "connect_visa",  # dangerous name
    # "discover_modules",  # no visible command
    # "drain_errors",  # no visible command
    # "fetch",  # no visible command
    # "fetch_power",  # no visible command
    # "get_current_setpoint",  # no visible command
    # "get_load_current",  # dangerous name
    # "get_load_mode",  # dangerous name
    # "get_load_power",  # dangerous name
    # "get_load_resistance",  # dangerous name
    # "get_load_voltage",  # dangerous name
    # "get_output",  # dangerous name
    # "get_protection_status",  # dangerous name
    # "get_remote_state",  # dangerous name
    # "get_smu_output_off_mode",  # dangerous name
    # "get_status_snapshot",  # no visible command
    # "get_voltage_limit",  # no visible command
    # "input_off",  # no visible command
    # "input_on",  # no visible command
    # "is_ok",  # no visible command
    # "is_open",  # no visible command
    # "measure",  # no visible command
    # "measure_all",  # no visible command
    # "measure_power",  # no visible command
    # "output_off",  # dangerous name
    # "output_on",  # dangerous name
    # "passed",  # no visible command
    # "read_raw",  # no visible command
    # "reset",  # writes
    # "self_test",  # dangerous name
    # "shutdown_all",  # dangerous name
    # "success",  # no visible command
    # "wait",  # writes
]

[sign_off]
signed_off_by = ""      # your name; the sweep will not run without it
signed_off_date = ""    # ISO date
instrument_serial = ""  # which physical unit this was signed for
allow_writes = false    # leave false; see README before changing
