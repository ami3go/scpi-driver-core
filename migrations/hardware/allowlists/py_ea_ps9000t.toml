# Hardware sweep allowlist for py_ea_ps9000t.
#
# Generated by generate_allowlist.py, which proposes only methods the
# source proves send nothing but queries. Regenerating overwrites the
# proposal but never the signature block, so review the diff.
#
# The sweep refuses to run while signed_off_by is empty. Signing means:
# you have read the read_only list, you know what is connected to this
# instrument, and you accept these calls being made to it.

driver = "py_ea_ps9000t"

# Proposed by static analysis: public, no arguments, sends only queries,
# and not name-matched as dangerous. 29 methods.
read_only = [
    "check_communication",  # *IDN?
    "get_analog_reference_range",  # SYSTem:CONFig:ANAlog:REFerence?
    "get_analog_remsb_action",  # SYSTem:CONFig:ANAlog:REMSB:ACTion?
    "get_analog_remsb_level",  # SYSTem:CONFig:ANAlog:REMSB:LEVel?
    "get_communication_timeout",  # SYSTem:COMMunicate:TIMeout?
    "get_current",  # CURRent?
    "get_current_limits",  # CURRent:LIMit:HIGH?, CURRent:LIMit:LOW?
    "get_device_class",  # SYSTem:DEVice:CLASs?
    "get_lan_control_port",  # SYSTem:COMMunicate:LAN:CONTrol?
    "get_lan_dns1",  # SYSTem:COMMunicate:LAN:DNS1?
    "get_lan_dns2",  # SYSTem:COMMunicate:LAN:DNS2?
    "get_lan_domain",  # SYSTem:COMMunicate:LAN:DOMain?
    "get_lan_gateway",  # SYSTem:COMMunicate:LAN:GATeway?
    "get_lan_hostname",  # SYSTem:COMMunicate:LAN:HOSTname?
    "get_lan_ip_address",  # SYSTem:COMMunicate:LAN:ADDRess?
    "get_lan_mac_address",  # SYSTem:COMMunicate:LAN:MAC?
    "get_lan_subnet_mask",  # SYSTem:COMMunicate:LAN:SMASk?
    "get_lan_timeout",  # SYSTem:COMMunicate:LAN:TIMeout?
    "get_measured_current",  # MEASure:CURRent?
    "get_measured_power",  # MEASure:POWer?
    "get_measured_values",  # MEASure:ARRay?
    "get_measured_voltage",  # MEASure:VOLTage?
    "get_nominal_ratings",  # SYSTem:NOMinal:CURRent?, SYSTem:NOMinal:POWer?, SYSTem:NOMinal:VOLTage?
    "get_power",  # POWer?
    "get_power_limit_high",  # POWer:LIMit:HIGH?
    "get_user_text",  # SYSTem:CONFig:USER:TEXT?
    "get_voltage",  # VOLTage?
    "get_voltage_limits",  # VOLTage:LIMit:HIGH?, VOLTage:LIMit:LOW?
    "identify",  # *IDN?
]

# Not called by the sweep. Each either writes to the instrument, sends
# nothing this script can see, or is name-matched as dangerous. Promote
# one into read_only only deliberately, and only with a reason recorded
# here. 28 methods.
needs_review = [
    # "acquire_remote_control",  # writes
    # "as_dict",  # no visible command
    # "client",  # no visible command
    # "close",  # dangerous name
    # "connect_simulated",  # dangerous name
    # "connected",  # dangerous name
    # "disable_output",  # writes
    # "enable_output",  # writes
    # "get_adjustment_limits",  # no visible command
    # "get_alarm_counters",  # dangerous name
    # "get_lan_dhcp_enabled",  # dangerous name
    # "get_lan_keepalive_enabled",  # dangerous name
    # "get_output_restore_mode",  # dangerous name
    # "get_overcurrent_protection",  # dangerous name
    # "get_overpower_protection",  # dangerous name
    # "get_overtemperature_alarm_action",  # dangerous name
    # "get_overvoltage_protection",  # dangerous name
    # "get_power_fail_alarm_action",  # dangerous name
    # "get_power_stage_after_remote",  # dangerous name
    # "get_protection_thresholds",  # dangerous name
    # "get_remote_control_owner",  # dangerous name
    # "is_open",  # no visible command
    # "is_output_enabled",  # dangerous name
    # "open",  # no visible command
    # "release_remote_control",  # writes
    # "resource",  # dangerous name
    # "simulator",  # no visible command
    # "timeout_s",  # no visible command
]

[sign_off]
signed_off_by = ""      # your name; the sweep will not run without it
signed_off_date = ""    # ISO date
instrument_serial = ""  # which physical unit this was signed for
allow_writes = false    # leave false; see README before changing
