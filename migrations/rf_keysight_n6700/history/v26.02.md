# Release History — v26.02

**Date:** 2026-07-17  
**Archive:** `rf_keysight_n6700_v26.02.zip`  
**Fixed internal root:** `rf_keysight_n6700/`  
**Python package version:** `26.2.0`

## Purpose

This update strengthens conformance with the Robot Framework Driver project standard and prevents future release packages from silently drifting away from the required structure.

## Changes

1. Added `PROJECT_REQUIREMENTS.md` with an explicit requirement-to-file mapping.
2. Added machine-readable `RELEASE_INFO.json` for release tooling and external automation.
3. Added `scripts/verify_project_package.py` to validate source trees and final ZIP archives.
4. Added BAT, PowerShell, and POSIX launchers for the project-package verifier.
5. Updated `scripts/make_release.py` to derive `vYY.RR` from the Python package version instead of maintaining an independent hard-coded release number.
6. Added archive verification after release creation, including single-root and fixed-root checks.
7. Added automated checks for required history, review, guides, README, GitHub Pages workflow, launch scripts, example count, current distribution artifacts, and the prohibited `src/` layout.
8. Added a unit test that executes the project verifier against the repository.
9. Updated CI to run package-standard verification on every supported Python/OS job.
10. Updated README, GitHub Pages, package manifest, release documentation, version metadata, Libdoc, checksums, wheel, and source distribution for v26.02.
11. Retained all v26.01 history and review records for traceability.

## Functional impact

No SCPI command behavior or public Robot Framework keyword behavior was intentionally changed. This is a packaging, release-integrity, documentation, and verification update.

## Compatibility

- Fixed unpacked folder remains `rf_keysight_n6700/`.
- Existing Robot Framework suites remain compatible.
- Python package version advances from `26.1.0` to `26.2.0`.
