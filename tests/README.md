# Tests

The test suite will be added phase-by-phase with the implementation task.

Planned groups:

- unit tests
- reusable transport contract tests
- local integration tests using simulated endpoints
- no physical hardware requirement in default CI
